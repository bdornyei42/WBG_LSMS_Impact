# generated pipeline output
LSMS_papers_*.xlsx
LSMS_papers_*.csv
LSMS_papers_*.png
# ...except the one the daily GitHub Actions update overwrites in place, which
# is tracked so the next scheduled run has something to diff against.
!LSMS_papers_latest.xlsx
!LSMS_papers_latest.csv
~$*.xlsx
~$*.xlsm
~$*.docx

# holds the OpenAlex API key -- keep it out of the repo
pipeline_config.json

# python
__pycache__/
*.pyc

# IDE
.idea/dbnavigator.xml

# scratch / backups
discarded_python/
discarded_excels/
